type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Car Importers",
  site_description:
    "Premium imported vehicles at competitive prices — Car Importers UK",
  site_domain: "https://carimporters.co.uk",
};
